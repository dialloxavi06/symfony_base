<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Clothing and Shoes</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <!-- Add your custom styles here -->
    <style>
        body {
            padding-top: 70px; /* Adjust based on your design */
        }

        .navbar {
            background-color: #87CEEB; /* Bleu ciel */
        }

        .navbar-brand {
            font-size: 2rem; /* Taille de la police plus grande */
            color: #333; /* Couleur du texte de la marque */
        }

        .navbar-nav .nav-item {
            margin-right: 15px;
        }

        .nav-link {
            color: #333;
            transition: color 0.3s;
            font-size: 1.2rem; /* Taille de la police plus grande */
        }

        .nav-link:hover,
        .nav-link.active {
            color: #007bff; /* Bootstrap primary color */
        }
    </style>
</head>

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                {% if error %}
                    <div class="alert alert-danger" role="alert">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                <form action="{{ path('app_login') }}" method="post" class="form-signin">
                    <h2 class="mb-3">Connexion</h2>

                    <label for="username" class="sr-only">Email:</label>
                    <input type="text" id="username" name="_username" class="form-control mb-3" placeholder="Email" value="{{ last_username }}" required autofocus>

                    <label for="password" class="sr-only">Mot de passe:</label>
                    <input type="password" id="password" name="_password" class="form-control mb-3" placeholder="Mot de passe" required>

                    {# If you want to control the URL the user is redirected to on success
                    <input type="hidden" name="_target_path" value="/account"> #}

                    <button type="submit" class="btn btn-primary btn-block">Connexion</button>
                </form>
                  <p class="mt-3">
                    Vous n'avez pas un compte ? <a href="{{ path('app_register') }}">inscrivez-vous ici</a>.
                </p>
            </div>
        </div>
    </div>
{% endblock %}
