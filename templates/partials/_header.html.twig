<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Clothing and Shoes</title>
    <!-- Liens vers les bibliothèques Bootstrap et Font Awesome -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <!-- Vos styles personnalisés -->
    <style>
        body {
            padding-top: 70px; /* Ajustez en fonction de votre conception */
        }

        .navbar {
            background-color: #87CEEB; /* Bleu ciel */
        }

        .navbar-brand {
            font-size: 2rem; /* Taille de la police plus grande */
            color: #333; /* Couleur du texte de la marque */
        }

        .navbar-nav .nav-item {
            margin-right: 15px;
        }

        .nav-link {
            color: #333;
            transition: color 0.3s;
            font-size: 1.2rem; /* Taille de la police plus grande */
        }

        .nav-link:hover,
        .nav-link.active {
            color: #007bff; /* Couleur primaire de Bootstrap */
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home.index') }}" id="home-link"><i class="fas fa-home"></i> Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('vetement_index') }}" id="page1-link"><i class="fas fa-tshirt"></i> Vêtements</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_chaussure') }}" id="page2-link"><i class="fas fa-shoe-prints"></i> Chaussures</a>
                </li>
                <!-- Liens avec des icônes pour l'inscription et la connexion -->
               
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}"><i class="fas fa-sign-in-alt"></i> Déconnexion</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Votre contenu va ici -->

<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
<script>
    $(document).ready(function () {
        $('.navbar-nav .nav-link').click(function () {
            $('.navbar-nav .nav-link').removeClass('active');
            $(this).addClass('active');
        });
    });
</script>
</body>
</html>
